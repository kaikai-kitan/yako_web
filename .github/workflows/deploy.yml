# Vercel への移行により、このワークフローは無効化しています。
# デプロイは Vercel が GitHub の main ブランチへの push を検知して自動実行します。

name: Deploy (disabled - using Vercel)

on:
  workflow_dispatch: # 手動実行のみ（自動トリガーなし）

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by Vercel."
